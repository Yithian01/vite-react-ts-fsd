# 파일명: Dockerfile.dev
FROM node:22-alpine

WORKDIR /app

# package.json만 먼저 복사해서 의존성 설치 (캐시 활용)
COPY package.json package-lock.json ./
RUN npm install

# 소스 코드는 복사하지 않음 (docker-compose에서 볼륨으로 연결)

EXPOSE 5173

# Vite를 외부(Host)에서 접속 가능하게 --host 옵션 추가 필수
CMD ["npm", "run", "dev", "--", "--host"]